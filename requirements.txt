# Python standard library (bundled with Python 3.14.3; no pip install needed):
# logging, re, pathlib, datetime, typing, atexit, contextlib, os, collections
# python -m pip install -r requirements.txt
ijson>=3.5.0
orjson>=3.11.9
psycopg>=3.3.4
psycopg-binary>=3.3.4
psycopg-pool>=3.3.1
python-dateutil>=2.9.0.post0
python-dotenv>=1.2.2
six>=1.17.0
typing_extensions>=4.15.0